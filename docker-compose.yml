services:
  pomodoro:
    build: .
    container_name: pomodoro-timer
    ports:
      - "3001:3001"
    volumes:
      - pomodoro-data:/app/backend/data
    restart: unless-stopped

volumes:
  pomodoro-data:
